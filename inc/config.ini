[database]
host = ${DATABASE_HOST}
port = ${DATABASE_PORT}
user = ${DATABASE_USER}
password = ${DATABASE_PASSWORD}
name = ${DATABASE_NAME}

[general]
environment = production
debug = false
basepath = /
